# Reflex Next 后端生产化目标

更新时间：2026-09-12

本文描述后端达到 `v1.0.0` 生产级状态的长期目标、验证标准与完成定义。
阶段性进度见 [后端生产化路线图](BACKEND-PRODUCTION-ROADMAP.md) 与 [更新日志](../CHANGELOG.md)。

## 1. 目标理解

- 目标是可正式推广的完整桌面产品后端，不是只有测试通过的开发原型；
- 当前项目采用 Python Core + Python Runtime/插件 + Tauri 2/Rust Host；
- 旧 PyQt 项目只作为行为基线，不得迁回旧 UI 架构；
- 当前 Core、Runtime、插件和 Rust Host 已有较强离线测试，匿名 Cloud 额度、反馈和质量发布闭环已实现，主要缺口是生产安全、真实 Provider、长期运行、可观测性、Cloud 多实例、升级回滚和发布制度；
- 前端与后端以稳定、版本化、可测试的契约解耦，契约变更需同步交付兼容说明。

开始相关工作前建议先阅读：

1. [AGENTS.md](../AGENTS.md)
2. [架构设计](ARCHITECTURE.md)
3. [功能迁移](MIGRATION.md)
4. [后端生产化路线图](BACKEND-PRODUCTION-ROADMAP.md)
5. 与当前任务直接相关的源码、测试和验证记录

## 2. 目标地图

- 终极愿景：个人用户能够安全、稳定地使用本地桌面 AI 文本工具，并能理解 Provider、隐私、历史和本地模型边界；
- 当前阶段：`v0.7.0-alpha.8` 生产化收尾。阶段 0/1/2 已关闭，Cloud Beta、质量发布/回滚的 PostgreSQL 多实例和当前提交的本地供应链证据已完成；输出额度结算的 PostgreSQL 多实例并发仍待复验，阶段 3 也仍需正式 72 小时稳定性测试（soak）；
- 近期目标：完成 72 小时稳定性测试、Windows 10 生命周期和真实旧版本升级验证；
- 中期方向：安全与本地数据、性能与长期运行、脱敏可观测性；
- 发布方向：安装、升级、回滚、代码签名、受控试用和 `v1.0.0`；
- 长期维护：Provider 兼容、依赖安全、季度稳定性测试、协议与数据兼容审查；
- 非目标：云端账号、云同步、强制遥测、默认重模型、公开插件市场、Core 重写；可选 Cloud 服务不改变本地 Core 边界。

## 3. 工作方式

- 每项变更保持外科式改动，不做无关重构、换栈、批量格式化或清理；
- 按安全、可靠性、性能、测试发布拆分工作包，模块边界互不冲突，公共协议由维护者裁决；
- 先写目标测试，再实现；统一后端门禁通过后才宣称完成，涉及桌面、安装或真实 Provider 时补充实机证据；
- 已通过的行为契约必须用测试保护，不能用文档声明替代实现证据。

## 4. 执行约束

- 不修改或暂存未跟踪 `resources/`；
- Core 不得依赖 PyQt、Tauri API、SQLite 默认初始化、pyperclip、torch、sentence-transformers 或 huggingface-hub；
- Provider、历史、翻译、批处理、Markdown、语义模型和桌面能力继续保持插件/宿主边界；
- 不读取、打印、记录、提交或硬编码真实 API Key、token、账号或凭据；真实冒烟必须使用新生成并由用户通过设置页保存的凭据；
- 不执行 `git reset --hard`、强制 checkout、递归删除、数据库清空、密钥轮换或不可逆发布操作；
- 不自动引入强制云端遥测、上传诊断或改变隐私默认值；可选质量分析和改进计划必须默认关闭并逐项授权。

## 5. 验证标准

- Python：按包隔离执行 Core、Runtime 和全部插件测试；
- Rust：限制为最多 2 个测试线程；
- TypeScript：运行 Domain Bridge 测试和生产构建；
- 安全：依赖审计、权限矩阵、敏感信息扫描、恶意输入夹具；
- 可靠性：真实 Provider 冒烟、取消、超时、重试、Sidecar 崩溃恢复；
- 性能：冷启动、首状态、取消时延、10,000 次 Mock 长稳测试、1,000 次混合调用；
- 数据：历史损坏、备份恢复、密钥轮换中断、导出取消；
- Cloud 数据：授权删除、质量发布来源、PostgreSQL 多实例发布/回滚和备份恢复；
- 发布：干净 Windows 构建、安装、覆盖安装、卸载、重装、回滚和包审计。

每次宣称通过时必须记录实际命令、退出码、测试数量、产物路径或实机证据。不能把静态检查当作完整运行证据。

## 6. 完成标准

- [后端生产化路线图](BACKEND-PRODUCTION-ROADMAP.md) 的全部 `v1.0` 门禁通过；
- Cloud Beta 的质量发布/回滚 PostgreSQL 多实例门禁有独立历史证据；输出额度结算的 PostgreSQL 多实例和公开部署门禁仍需独立证据；
- 自动测试、真实 Provider、长期稳定性测试、数据恢复和安装生命周期均有证据；
- P0/P1 安全与可靠性问题为 0；
- 版本、安装包、标签、校验和、SBOM 和发布说明一致；
- 正式包可验证来源、可安装、可升级、可卸载、可回滚；
- 前端可以只依赖稳定公开契约完成集成；
- 用户和支持人员能通过脱敏错误与诊断处理常见问题；
- `v1.0.0` 已提交、推送、打标签并完成发布包审计。

## 7. 需要维护者决策的事项

推进过程中遇到以下情况时暂停并交由维护者决策，不自行取舍：

- 需要提供或采购新凭据、代码签名证书、付费服务或外部账号；
- 需要不可逆删除历史、配置、模型或发布物；
- 需要改变隐私、遥测、商业或 Provider 策略；
- 项目规则与目标冲突，或连续两轮验证失败且存在互斥取舍；
- 当前操作会影响第三方权益或执行未经授权的外部写入。
